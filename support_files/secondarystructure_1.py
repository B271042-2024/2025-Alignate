# perform secondary structure
# transfer to GUI
# annotation for components on secondary structure with %similarity against ref
from PySide6.QtWidgets import QWidget, QVBoxLayout, QHBoxLayout, QSlider
from alignate import 

class secondarystructure(QWidget):
    def __init__(self):
        super().__init__()

        