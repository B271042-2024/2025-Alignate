#!/usr/bin/env bash
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export EMBOSS_DATA="$HERE/share/data"
export EMBOSS_ACDROOT="$HERE/share/acd"
export LD_LIBRARY_PATH="$HERE/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
exec "$HERE/bin/tranalign.bin" "$@"
